{
  "rules": [
    {
      "pattern": "^(did:ion:bank_issuer)$",
      "url": "https://yankee.openvino.org/files/dids/did-ion-bank-issuer.json"
    },
    { "pattern": "^(did:key:.+)$" },
    { "pattern": "^(did:web:.+)$" }
  ]
}
